# Component BA — Local Agent Application Evidence

Date: 2026-08-13

## Scope

BA composes authentication, model configuration, attestation, qualification,
loopback sandbox/transport, reasoning, durable sessions, replay, and the Agent
application behind the default server command.

## Integrity boundary

BA makes planning and human review genuinely runnable. It does not register a
fake universal tool or claim autonomous execution. The already-qualified runtime
can be composed only with real typed tools and verifiers appropriate to the
operator's work.

## Verification

Focused composition, restart, missing-input, CLI, and model configuration gates
passed. The exact release-evidence pipeline passed, including Python/TypeScript,
formatting, lint, strict typing, audits, builds, and clean-room gates. The full
Python suite passed 456 tests. Source and wheel builds, a fresh wheel install,
the installed server help command, and installed composition import also passed.
