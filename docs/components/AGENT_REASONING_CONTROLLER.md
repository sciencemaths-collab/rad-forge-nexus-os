# Component AR: Agent Reasoning Controller

The controller is a proposal-only composition service. It requires exact current model
qualification, sends bounded objective context through the provider-neutral adapter,
validates strict JSON, supplies all trusted identity/digest fields, and commits only a
fully valid candidate through Phase AQ. One safe bounded repair is optional.
