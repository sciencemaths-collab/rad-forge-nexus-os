schema_version: "1.0"
project_id: bad_secret
name: Invalid secret fixture
mode: app_build
goal: This must fail before side effects.
workspace: {root: ./workspace}
providers:
  implementer:
    adapter: openai
    credential: sk-literal-secrets-are-forbidden
policy:
  max_attempts: 1
  max_elapsed_seconds: 60
  max_cost_usd: 0
  require_approval: [SENSITIVE, DESTRUCTIVE]
acceptance:
  - {id: MUST_FAIL, description: Invalid fixture, verifier: schema}

